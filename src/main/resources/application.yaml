server:
  port: 8081

spring:
  application:
    name: UserMS
  datasource:
    url: jdbc:postgresql://localhost:5432/matchai
    username: rohitpounikar
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
  data:
    redis:
      host: localhost
      port: 6379
      timeout: 2000ms

jwt:
  secret: your-secret-key-change-this-in-production
  expiration: 86400000  # 24 hours in milliseconds
